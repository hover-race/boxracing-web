# TODO 1: Record laps
 Record {x, y, z} per frame
 Compute cumulative distance
 Store normalized u = dist / lapLength
 Output array: {u, x, y, z}

On every lap, download a file lap-1.json with normalized coords

add a gui option to record, default on
